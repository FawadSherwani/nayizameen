"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { Edit3, Eye, MapPin, Plus, Search, Trash2, X } from "lucide-react";
import { featuredProjects as defaultProjects } from "@/lib/data";

const emptyProject = { title: "", location: "", price: "", badge: "NEW LAUNCH", image: "" };

function ProjectField({ label, name, value, onChange, required = false }) {
  return <label className="block"><span className="mb-1.5 block text-xs font-bold text-slate-600">{label}</span><input name={name} value={value} onChange={onChange} required={required} className="h-10 w-full rounded-lg border border-slate-200 bg-white px-3 text-sm outline-none focus:border-primary-700 focus:ring-2 focus:ring-primary-100" /></label>;
}

export default function AdminProjectsPage() {
  const [items, setItems] = useState(() => { if (typeof window === "undefined") return defaultProjects; try { return JSON.parse(window.localStorage.getItem("nayi-zameen-admin-projects")) || defaultProjects; } catch { return defaultProjects; } });
  const [query, setQuery] = useState("");
  const [formOpen, setFormOpen] = useState(false);
  const [editingTitle, setEditingTitle] = useState(null);
  const [form, setForm] = useState(emptyProject);
  const [notice, setNotice] = useState("");
  useEffect(() => { window.localStorage.setItem("nayi-zameen-admin-projects", JSON.stringify(items)); }, [items]);
  const filtered = useMemo(() => items.filter((item) => (item.title + " " + item.location).toLowerCase().includes(query.toLowerCase())), [items, query]);
  const update = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const openCreate = () => { setEditingTitle(null); setForm(emptyProject); setFormOpen(true); };
  const openEdit = (item) => { setEditingTitle(item.title); setForm({ ...emptyProject, ...item }); setFormOpen(true); };
  const save = (event) => { event.preventDefault(); const record = { ...form }; setItems((current) => editingTitle ? current.map((item) => item.title === editingTitle ? record : item) : [record, ...current]); setFormOpen(false); setNotice(editingTitle ? "Project updated successfully." : "Project added successfully."); };
  const remove = (item) => { if (!window.confirm("Delete " + item.title + "?")) return; setItems((current) => current.filter((entry) => entry.title !== item.title)); setNotice("Project deleted."); };

  return <><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold text-primary-700">DEVELOPMENT MANAGEMENT</p><h1 className="mt-1 text-3xl font-extrabold text-slate-900">Projects</h1><p className="mt-2 text-sm text-slate-500">Manage featured developments shown across the portal.</p></div><button type="button" onClick={openCreate} className="inline-flex items-center gap-2 rounded-xl bg-primary-700 px-4 py-3 text-sm font-bold text-white shadow-lg shadow-primary-700/20 hover:bg-primary-800"><Plus className="h-4 w-4" />Add Project</button></div>{notice && <p role="status" className="mt-5 rounded-xl border border-primary-100 bg-primary-50 px-4 py-3 text-sm font-semibold text-primary-800">{notice}</p>}<section className="mt-8 rounded-2xl bg-white p-5 shadow-sm ring-1 ring-slate-100"><label className="flex h-11 max-w-lg items-center gap-2 rounded-xl border border-slate-200 px-3"><Search className="h-4 w-4 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search projects or locations..." className="w-full bg-transparent text-sm outline-none" /></label><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-y border-slate-100 bg-slate-50 text-xs uppercase tracking-wide text-slate-400"><tr><th className="px-4 py-3">Project</th><th className="px-4 py-3">Location</th><th className="px-4 py-3">Starting price</th><th className="px-4 py-3">Badge</th><th className="px-4 py-3 text-right">Actions</th></tr></thead><tbody>{filtered.map((item) => <tr key={item.title} className="border-b border-slate-100 last:border-0"><td className="px-4 py-4 font-bold text-slate-800">{item.title}</td><td className="px-4 py-4 text-slate-600"><span className="flex items-center gap-1"><MapPin className="h-4 w-4 text-primary-700" />{item.location}</span></td><td className="px-4 py-4 font-semibold text-primary-700">{item.price}</td><td className="px-4 py-4"><span className="rounded-full bg-primary-50 px-2.5 py-1 text-xs font-bold text-primary-700">{item.badge}</span></td><td className="px-4 py-4"><div className="flex justify-end gap-1"><Link href="/projects" className="rounded-lg p-2 text-slate-500 hover:bg-primary-50 hover:text-primary-700" aria-label="View projects"><Eye className="h-4 w-4" /></Link><button type="button" onClick={() => openEdit(item)} className="rounded-lg p-2 text-slate-500 hover:bg-primary-50 hover:text-primary-700" aria-label={"Edit " + item.title}><Edit3 className="h-4 w-4" /></button><button type="button" onClick={() => remove(item)} className="rounded-lg p-2 text-slate-500 hover:bg-rose-50 hover:text-rose-600" aria-label={"Delete " + item.title}><Trash2 className="h-4 w-4" /></button></div></td></tr>)}</tbody></table></div>{filtered.length === 0 && <p className="py-12 text-center text-sm text-slate-500">No projects match your search.</p>}</section>{formOpen && <div className="fixed inset-0 z-50 flex items-start justify-center overflow-y-auto bg-slate-900/50 p-4 md:items-center"><form onSubmit={save} className="w-full max-w-xl rounded-2xl bg-white p-6 shadow-2xl md:p-8"><div className="flex items-start justify-between"><div><p className="text-sm font-semibold text-primary-700">PROJECT DETAILS</p><h2 className="mt-1 text-2xl font-extrabold text-slate-900">{editingTitle ? "Edit project" : "Add new project"}</h2></div><button type="button" onClick={() => setFormOpen(false)} aria-label="Close form"><X className="h-5 w-5 text-slate-400" /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="sm:col-span-2"><ProjectField label="Project title" name="title" value={form.title} onChange={update} required /></div><ProjectField label="Location" name="location" value={form.location} onChange={update} required /><ProjectField label="Starting price" name="price" value={form.price} onChange={update} required /><ProjectField label="Badge" name="badge" value={form.badge} onChange={update} required /><div className="sm:col-span-2"><ProjectField label="Image URL" name="image" value={form.image} onChange={update} required /></div></div><div className="mt-6 flex justify-end gap-3 border-t border-slate-100 pt-5"><button type="button" onClick={() => setFormOpen(false)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-bold text-slate-600">Cancel</button><button type="submit" className="rounded-xl bg-primary-700 px-5 py-2.5 text-sm font-bold text-white hover:bg-primary-800">{editingTitle ? "Save changes" : "Add project"}</button></div></form></div>}</>;
}
